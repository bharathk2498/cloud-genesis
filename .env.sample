# Sample environment variables for Cloud Genesis
# Copy this file to `.env` and fill in the values for your environment.

# AWS configuration
AWS_PROFILE=default
AWS_REGION=us-east-1

# Azure configuration
AZURE_SUBSCRIPTION_ID=<subscription-id>
AZURE_TENANT_ID=<tenant-id>

# GCP configuration
GCP_PROJECT_ID=<project-id>
GCP_REGION=us-central1

# General parameters
ORG_ID=<organisation-id>
TF_VAR_state_bucket=<name-of-terraform-state-bucket>

# API keys and secrets should be stored in a secure secret manager and
# referenced here through environment variables or CLI profiles.